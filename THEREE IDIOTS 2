#include <stdio.h>

int main() {
    int x1, y1, x2, y2;
    scanf("%d %d %d %d", &x1, &y1, &x2, &y2);


    float mx = (x1 + x2) / 2.0f;
    float my = (y1 + y2) / 2.0f;


    printf("Binoy's house is located at (%.1f,%.1f)\n", mx, my);

    return 0;
}
